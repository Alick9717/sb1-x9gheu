import './scene/SceneManager.js';
import './controls/ControlsManager.js';
import './upload/FileUploadManager.js';

// Initialize the application
document.addEventListener('DOMContentLoaded', () => {
  // The managers are automatically initialized through their imports
  console.log('3D Model Viewer initialized');
});